services:
  service:
    build: .
    container_name: linebot-service
    ports:
      - "8000:8000"  # 映射 FastAPI 服務的端口
    env_file:
      - .env
    volumes:
      - .:/app  # 將當前目錄映射到容器中的 /app 目錄

# volumes:
  # app-data: